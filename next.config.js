/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Las imágenes de ejercicios (catálogo / Storage) se habilitarán en Fase 2.
  // images: { remotePatterns: [...] },
};

module.exports = nextConfig;
